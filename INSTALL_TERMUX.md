# Установка Home Cloud на Android (Termux)

## Требования
- Android устройство (Redmi 14C или любой другой)
- Termux из F-Droid (НЕ из Google Play!)
- Минимум 2 GB свободного места

## Шаг 1: Установка Termux

1. Скачайте Termux из F-Droid:
   - Откройте https://f-droid.org/packages/com.termux/
   - Нажмите "Download APK"
   - Установите приложение

2. (Опционально) Установите Termux:Boot для автозапуска:
   - https://f-droid.org/packages/com.termux.boot/

## Шаг 2: Первоначальная настройка Termux

Откройте Termux и выполните команды:

```bash
# Обновить пакеты
pkg update && pkg upgrade -y

# Установить необходимые пакеты
pkg install python git clang libjpeg-turbo libpng zlib -y

# ВАЖНО: Разрешить доступ к хранилищу телефона
termux-setup-storage
```

**⚠️ КРИТИЧЕСКИ ВАЖНО:** 
После команды `termux-setup-storage` Android покажет диалог разрешений:
- **ОБЯЗАТЕЛЬНО нажмите "Разрешить" / "Allow"**
- Без этого не будет доступа к ~/storage/

Проверьте что storage появился:
```bash
ls ~/storage/
# Должны увидеть: dcim  downloads  movies  music  pictures  shared
```

Если папки `~/storage/` нет или пустая:
```bash
# Повторите настройку
termux-setup-storage
# И снова дайте разрешение в диалоге Android
```

## Шаг 3: Клонирование проекта

```bash
# Перейти в домашнюю папку
cd ~

# Клонировать проект с GitHub
git clone https://github.com/furiksayram-commits/cloud.git

# Перейти в папку проекта
cd cloud
```

## Шаг 4: Настройка хранилища

**ВАЖНО: Настроить хранилище ПЕРЕД установкой Python пакетов!**

Вы можете использовать разные места для хранения файлов:

### Вариант А: Внутренняя память телефона (Рекомендуется)
```bash
# ПРАВИЛЬНЫЙ способ для Termux:
# storage/shared это read-only монтирование
# Создаём папку через реальный путь:

# Вариант 1: Использовать Downloads
mkdir -p ~/storage/downloads/HomeCloud
ln -sf ~/storage/downloads/HomeCloud ~/cloud/storage

# Вариант 2: Использовать Documents (если есть)
mkdir -p ~/storage/documents/HomeCloud
ln -sf ~/storage/documents/HomeCloud ~/cloud/storage

# Вариант 3: Использовать DCIM (папка камеры)
mkdir -p ~/storage/dcim/HomeCloud
ln -sf ~/storage/dcim/HomeCloud ~/cloud/storage
```

**Рекомендую**: `~/storage/downloads/HomeCloud` - это стандартная папка Загрузки Android

### Вариант Б: SD карта (если есть)
```bash
# Найти путь к SD карте
ls ~/storage/

# Создать папку на SD карте
mkdir -p ~/storage/external-1/HomeCloud

# Создать символическую ссылку
ln -sf ~/storage/external-1/HomeCloud ~/cloud/storage
```

### Вариант В: Папка в Termux (меньше места)
```bash
# Создать папку внутри Termux
mkdir -p ~/cloud/storage
```

**Проверить что storage создана:**
```bash
ls -la ~/cloud/storage
```

## Шаг 5: Настройка Python окружения

```bash
# Создать виртуальное окружение
python -m venv .venv

# Активировать виртуальное окружение
source .venv/bin/activate

# Обновить pip и setuptools
pip install --upgrade pip setuptools wheel

# Установить Pillow с правильными флагами для Termux
CFLAGS="-I$PREFIX/include" LDFLAGS="-L$PREFIX/lib" pip install --no-cache-dir pillow

# Установить остальные зависимости
pip install flask werkzeug
```

**Важно**: Pillow в Termux требует специальных флагов компиляции!

## Шаг 5: Настройка хранилища

Уже настроено на Шаге 4, но если пропустили:
```bash
mkdir -p ~/storage/shared/HomeCloud
ln -sf ~/storage/shared/HomeCloud ~/cloud/storage
```

## Шаг 6: Запуск сервера

```bash
# Убедитесь что вы в папке проекта и активировали venv
cd ~/cloud
source .venv/bin/activate

# Запустить сервер
python app.py
```

Вы увидите что-то вроде:
```
✓ PWA иконки созданы
* Running on all addresses (0.0.0.0)
* Running on http://127.0.0.1:3000
* Running on http://192.168.1.XXX:3000
```

## Шаг 7: Доступ к облаку

### На самом телефоне:
Откройте браузер и перейдите на:
```
http://localhost:3000
```

### С других устройств в той же WiFi сети:
1. Узнайте IP адрес телефона:
```bash
# Установить termux-api
pkg install termux-api

# Узнать IP
termux-wifi-connectioninfo | grep ip
```

2. На другом устройстве откройте:
```
http://IP_АДРЕС_ТЕЛЕФОНА:3000
```

## Автозапуск при загрузке телефона

### Вариант 1: Termux:Boot (Рекомендуется)

1. Установите Termux:Boot из F-Droid
2. Создайте скрипт автозапуска:

```bash
# Создать папку для скриптов загрузки
mkdir -p ~/.termux/boot

# Создать скрипт
cat > ~/.termux/boot/start-cloud.sh << 'EOF'
#!/data/data/com.termux/files/usr/bin/bash
termux-wake-lock
cd ~/cloud
source .venv/bin/activate
python app.py > ~/cloud/server.log 2>&1 &
EOF

# Сделать скрипт исполняемым
chmod +x ~/.termux/boot/start-cloud.sh
```

3. Перезагрузите телефон - сервер запустится автоматически

### Вариант 2: Фоновый запуск вручную

```bash
# Установить termux-services
pkg install termux-services

# Перезапустить Termux после установки
exit
# (откройте Termux заново)

# Запустить в фоне
cd ~/cloud
source .venv/bin/activate
nohup python app.py > server.log 2>&1 &

# Предотвратить засыпание
termux-wake-lock
```

## Полезные команды

### Проверить что сервер работает:
```bash
ps aux | grep python
```

### Остановить сервер:
```bash
pkill -f "python app.py"
```

### Просмотреть логи:
```bash
tail -f ~/cloud/server.log
```

### Узнать использование порта:
```bash
pkg install net-tools
netstat -tulpn | grep 3000
```

### Обновить проект с GitHub:
```bash
cd ~/cloud
git pull
source .venv/bin/activate
pip install -r requirements.txt
```

## Оптимизация для экономии батареи

```bash
# В app.py изменить debug=False для production
# Уже настроено по умолчанию
```

## Доступ из интернета (опционально)

### Вариант 1: Tailscale VPN
```bash
pkg install tailscale
tailscale up
# Следуйте инструкциям для авторизации
# После этого сервер доступен по Tailscale IP из любой точки мира
```

### Вариант 2: Cloudflare Tunnel
```bash
pkg install cloudflared
cloudflared tunnel login
# Следуйте инструкциям
```

## Решение проблем

### Termux закрывается системой:
1. Отключите оптимизацию батареи для Termux:
   - Настройки → Приложения → Termux → Батарея → Без ограничений
2. Используйте `termux-wake-lock`
3. Держите телефон на зарядке

### Ошибка "Permission denied" при доступе к storage:
```bash
termux-setup-storage
# Дайте разрешение в диалоге Android
```

### Ошибка "Permission denied" при создании storage:
```bash
# ~/storage/shared это read-only!
# НЕ ИСПОЛЬЗУЙТЕ: mkdir -p ~/storage/shared/HomeCloud

# ПРАВИЛЬНО - используйте downloads:
mkdir -p ~/storage/downloads/HomeCloud

# Если downloads тоже не работает, проверьте доступные папки:
ls -la ~/storage/
# Ищите папки с правами на запись (writable)

# Попробуйте documents:
mkdir -p ~/storage/documents/HomeCloud

# Или используйте внутреннее хранилище Termux:
mkdir -p ~/cloud/storage
# (Но это займёт место внутри приложения Termux)
```

### Ошибка "FileExistsError: File exists: 'storage'":
```bash
# Удалить storage если это битая символическая ссылка
cd ~/cloud
rm storage

# Создать заново
mkdir -p ~/storage/shared/HomeCloud
ln -sf ~/storage/shared/HomeCloud ~/cloud/storage

# Проверить
ls -la ~/cloud/storage
```

### Ошибка "No module named 'PIL'":
```bash
# Удалить старое виртуальное окружение
rm -rf ~/cloud/.venv

# Создать заново
cd ~/cloud
python -m venv .venv
source .venv/bin/activate

# Установить зависимости правильно
pip install --upgrade pip setuptools wheel

# Установить Pillow с флагами компиляции
CFLAGS="-I$PREFIX/include" LDFLAGS="-L$PREFIX/lib" pip install --no-cache-dir pillow

# Установить остальное
pip install flask werkzeug

# Проверить установку
python -c "from PIL import Image; print('Pillow работает!')"
```

### Ошибка установки Pillow:
```bash
pkg install libjpeg-turbo libpng zlib -y
pip install --no-cache-dir pillow
```

### Порт 3000 занят:
```bash
# Изменить порт в app.py на другой (например, 8080)
nano ~/cloud/app.py
# Найти строку app.run(host='0.0.0.0', port=3000, debug=False)
# Изменить 3000 на 8080
```

## PWA установка на телефон

1. Откройте http://localhost:3000 в Chrome/Firefox
2. В меню браузера выберите "Добавить на главный экран"
3. Теперь можно запускать как обычное приложение!

## Характеристики производительности

- **Redmi 14C**: Отличная производительность ✅
- **Потребление RAM**: ~150-200 MB
- **Потребление батареи**: Минимальное при использовании wake-lock
- **Скорость загрузки**: Зависит от WiFi

## Безопасность

⚠️ **Важно**: Сервер доступен всем устройствам в вашей WiFi сети без пароля!

Для добавления аутентификации потребуется модификация кода.

---

**Готово! Теперь у вас есть домашнее облако на Android! ☁️📱**
