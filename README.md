# ☁️ Домашнее Облако

Простое и удобное локальное облачное хранилище для дома с поддержкой PWA, автосортировкой фото/видео и встроенным видеоплеером.

## 🚀 Возможности

- 📤 Загрузка файлов (несколько файлов одновременно)
- 📸 **Автосортировка фото по EXIF** (Фото/Год/Месяц)
- 🎬 **Автосортировка видео** (Видео/Год/Месяц)
- 📁 Создание папок
- 📂 Навигация по папкам
- ⬇️ Скачивание файлов
- 🗑️ Удаление файлов и папок
- ✏️ Переименование файлов и папок
- 🔍 **Глобальный рекурсивный поиск**
- 🖼️ **Предпросмотр изображений** с навигацией
- 🎬 **Встроенный видеоплеер** с автовоспроизведением
- 📱 **PWA поддержка** - установка как приложение
- � **Свайпы** для навигации по фото/видео
- 🌙 **Темная тема**
- �📊 Статистика хранилища
- 🎨 Красивый современный интерфейс
- 📱 Полная адаптация под мобильные устройства
- 🌐 Доступ из локальной сети

## 📋 Требования

- Python 3.7+
- Flask 3.x
- Pillow 12.x (для обработки EXIF)
- Werkzeug

## 🔧 Установка

### Windows (PowerShell):

```powershell
# 1. Клонировать репозиторий
git clone https://github.com/furiksayram-commits/cloud.git
cd cloud

# 2. Создать виртуальное окружение
python -m venv .venv
.\.venv\Scripts\Activate.ps1

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Запустить сервер
python app.py
```

### Linux/Raspberry Pi:

```bash
# 1. Клонировать репозиторий
git clone https://github.com/furiksayram-commits/cloud.git
cd cloud

# 2. Создать виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Запустить сервер
python app.py
```

### Android (Termux):

**Автоматическая установка (РЕКОМЕНДУЕТСЯ):**
```bash
curl -fsSL https://raw.githubusercontent.com/furiksayram-commits/cloud/main/install-termux.sh | bash
```
⚡ **Автоматически устанавливает Gunicorn для высокой производительности!**

**Ручная установка:**
См. подробную инструкцию: [INSTALL_TERMUX.md](INSTALL_TERMUX.md)

Быстрая версия:
```bash
pkg update && pkg upgrade -y
pkg install python git clang libjpeg-turbo -y
termux-setup-storage
git clone https://github.com/furiksayram-commits/cloud.git
cd cloud
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pip install gunicorn  # Для быстрой работы!
python app.py
```

**Производительность:**
- 🐌 Flask debug: очень медленно (для отладки)
- ⚡ Flask production: средняя скорость
- 🚀 **Gunicorn: быстро (рекомендуется для Termux!)**

См. подробности: [PERFORMANCE.md](PERFORMANCE.md)

## 🌐 Доступ
cd /home/user/home-cloud

# 2. Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Запустить сервер
python3 app.py
```

### Для Android (Termux):

```bash
# 1. Установить Python
pkg install python

# 2. Перейти в папку проекта
cd ~/home-cloud

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Запустить сервер
python app.py
```

## 🌐 Доступ к хранилищу

После запуска сервер будет доступен по адресу:

- На этом же устройстве: http://localhost:5000
- Из локальной сети: http://[IP-адрес]:5000

Чтобы узнать IP-адрес:
- **Windows**: `ipconfig` (ищите IPv4 адрес)
- **Linux/Raspberry Pi**: `ip addr` или `hostname -I`
- **Android (Termux)**: `ifconfig` или `ip addr`

## 📁 Структура проекта

```
home-cloud/
├── app.py              # Основной файл приложения
├── templates/
│   └── index.html      # HTML шаблон интерфейса
├── storage/            # Папка для хранения файлов (создается автоматически)
├── requirements.txt    # Зависимости Python
└── README.md          # Этот файл
```

## ⚙️ Настройка

В файле `app.py` можно изменить:

- `MAX_CONTENT_LENGTH` - максимальный размер загружаемого файла (по умолчанию 500 МБ)
- `UPLOAD_FOLDER` - папка для хранения файлов (по умолчанию 'storage')
- `ALLOWED_EXTENSIONS` - разрешенные расширения файлов
- `app.secret_key` - секретный ключ (измените для продакшена!)
- Порт и хост в `app.run()` (по умолчанию 0.0.0.0:5000)

## 🔒 Безопасность

**ВАЖНО!** Это базовая версия без аутентификации. Для использования в продакшене:

1. Смените секретный ключ `app.secret_key`
2. Добавьте аутентификацию пользователей
3. Используйте HTTPS
4. Настройте файрвол
5. Установите `debug=False` в `app.run()`

## 🚀 Запуск как сервиса (для постоянной работы)

### Для Linux/Raspberry Pi (systemd):

Создайте файл `/etc/systemd/system/home-cloud.service`:

```ini
[Unit]
Description=Home Cloud Storage
After=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/home-cloud
ExecStart=/home/pi/home-cloud/venv/bin/python app.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl enable home-cloud
sudo systemctl start home-cloud
```

### Для Android (Termux):

Используйте `termux-wake-lock` для предотвращения засыпания:
```bash
termux-wake-lock
python app.py
```

## 📝 Лицензия

Свободное использование для личных целей.

## 🤝 Поддержка

При возникновении проблем проверьте:
- Установлены ли все зависимости
- Открыт ли порт 5000
- Правильно ли указан IP-адрес
- Запущен ли файрвол (и разрешен ли порт 5000)

---

Создано с ❤️ для домашнего использования
